# Snippet for ~/.config/hypr/hyprland.conf
#
# Recommended layer rule: keep the pet above normal windows but below
# notifications and the lock screen.

layerrule = blur, aymm
layerrule = ignorezero, aymm

exec-once = aymm
